#include <stdio.h>

int main() {
    int caixa_numero = 10;
    printf("Valor inicial na memoria: %d\n", caixa_numero);
    
    /* INSTRUCAO PARA O ALUNO: 
     Descomente a linha abaixo, tente compilar com GCC e leia o erro.
     O compilador nao permite colocar um texto na 'caixa' de inteiros.
    */
    
    // caixa_numero = "Engenharia"; 
    
    return 0;
}
